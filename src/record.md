```
Takeaway for today
1. 특정 컴포넌트에 CSS 사용 불가. Why?
렌더 되는게 컴포넌트 리턴이라서 > props 로 전달해야한다.
>> specific component CSS only >> pass props*

2. javascript has properties and value

3. props 사용 방법
In order to pass props in, we need to go back to where it’s being rendered from.
```
